using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    /*public GameObject draggingObject;
    public GameObject currentCell;

    public static GameManager instance;

    private void Awake(){
        instance = this;
    }

    public void PlaceObject(){
        if(draggingObject != null && currentCell != null){
            Instantiate(draggingObject.GetComponent<DragFunction>().card.object_Game, currentCell.transform);
            currentCell.GetComponent<ObjectContainer>().isFull = true;
        }
    }*/
}
